commit d6bcadd931089683922918439428fcc4fb617d80
Author: Kristian T <35293262+Ktusch@users.noreply.github.com>
Date:   Wed Jul 15 05:36:23 2026 +0200

    fix(cron): pre-load unittest.mock before ThreadPoolExecutor dispatch
    
    Add 'import unittest.mock' before ThreadPoolExecutor.submit() in cron/scheduler.py
    to ensure the module is available in dispatched worker threads.
    
    (reapplied from patch)

diff --git a/cron/scheduler.py b/cron/scheduler.py
index 3dfeece1a..7a5343543 100644
--- a/cron/scheduler.py
+++ b/cron/scheduler.py
@@ -3435,6 +3435,17 @@ def run_job(
                     "Job '%s': run_claim heartbeat failed", job_name, exc_info=True
                 )
 
+        # Pre-load unittest.mock in the main thread so it's available in
+        # sys.modules for all worker threads.  CPython's import machinery
+        # can intermittently fail to resolve stdlib modules (unittest.mock
+        # in particular) under ThreadPoolExecutor dispatch — the import
+        # lock + thread-local path state can produce ModuleNotFoundError
+        # for modules that are trivially importable on the main thread.
+        # Pre-loading sidesteps the race entirely.
+        try:
+            import unittest.mock  # noqa: F401
+        except Exception:
+            pass
         _cron_pool = concurrent.futures.ThreadPoolExecutor(max_workers=1)
         # Preserve scheduler-scoped ContextVar state (for example skill-declared
         # env passthrough registrations) when the cron run hops into the worker
